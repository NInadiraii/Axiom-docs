# Render.com infrastructure-as-code for the Speed-to-Lead LINE bot.
# Push this file to GitHub and connect your repo at render.com/new
# to get a one-click deploy with all env vars pre-configured.

services:
  - type: web
    name: line-lead-bot
    env: python
    plan: free                          # Upgrade to "starter" for always-on
    buildCommand: pip install -r requirements_line.txt
    startCommand: uvicorn line_lead_bot:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health

    envVars:
      # ── LINE ────────────────────────────────────────────────
      - key: LINE_CHANNEL_SECRET
        sync: false                     # Set manually in Render dashboard
      - key: LINE_CHANNEL_ACCESS_TOKEN
        sync: false

      # ── Email ───────────────────────────────────────────────
      - key: SMTP_HOST
        value: smtp.gmail.com
      - key: SMTP_PORT
        value: "465"
      - key: SMTP_USER
        sync: false
      - key: SMTP_PASS
        sync: false
      - key: ALERT_EMAIL_TO
        sync: false

      # ── Telegram ─────────────────────────────────────────────
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false
